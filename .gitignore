# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test / tool caches
.pytest_cache/
.ruff_cache/
*.html

# Deno-materialised deps for the static-KaTeX build tool (tools/katex/).
# The pin lives in tools/katex/deno.json (+ deno.lock); node_modules is a
# reproducible local cache, not committed.
node_modules/

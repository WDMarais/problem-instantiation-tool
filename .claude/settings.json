{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path | select(endswith(\".py\"))' | { read -r f; [ -n \"$f\" ] && uv run --project /home/sitrosi/coding-projects/problem-instantiation-tool ruff format \"$f\"; } 2>/dev/null || true",
            "statusMessage": "ruff format"
          }
        ]
      }
    ]
  }
}
